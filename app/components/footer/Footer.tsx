import PageBlock from '../pageBlock/PageBlock';
import styles from './Footer.module.css';

export default function Footer() {
  return <PageBlock className={styles.footer}>
    <h1>Lets get in touch</h1>
    <p>IDK what to put here yet</p>
  </PageBlock>
}